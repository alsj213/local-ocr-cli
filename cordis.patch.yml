# dsh bundle layer: mount the LocalOCR plugin (dsh/index.js via the package
# root export; bin stays the CLI, nothing imports the root otherwise).
- insert:
    - id: local-ocr
      name: 'local-ocr-cli'
